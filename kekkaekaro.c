#include <stdio.h>

int main ()
{
    int a;
    int b;

    printf("ïnserisci due numeri");
    scanf ("%d %d", &a , &b);
    int divisione = a / b;
    printf("ïl risultato della divisione è: %d\n ", divisione);
}